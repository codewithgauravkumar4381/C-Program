#include <stdio.h>
#include <conio.h>
//function declaration
void printmessage(int count, char message[]);
int main()
{
    //colling the  functhion
    printmessage(3,"hello.world");
    return 0;
}
// function definition
void printmessage(int count, char message[]);
{
    for (int i = 0;i< count; i++)
    {
        printf("%s\n",meesage);
    }
} 