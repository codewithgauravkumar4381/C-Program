#include <stdio.h>
#include <conio.h>
int main() 
{
    int a = 5, b = 3;
    int result = a ^ b;  
    // Bitwise XOR
    printf("Result of %d ^ %d = %d\n", a, b, result);   
    return 0;
}